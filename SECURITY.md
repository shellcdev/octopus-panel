# 安全政策（SECURITY）

## 支持的版本

| 版本 | 是否受支持 |
|---|---|
| v3.1.x（最新） | ✅ |
| < v3.1 | ❌（请先升级到最新版） |

## 报告漏洞

**请勿在公开 issue 中披露安全漏洞。**

请通过以下私报通道之一：
- **GitHub Security Advisory**：进入本仓库 → `Security` → `Report a vulnerability`；
- 或在仓库 Issues 中**私信维护者**并注明"安全漏洞"，由维护者转为私报。

我们会尽快确认、评估，并在修复后与你协调披露时间。

## 范围说明

本技能为纯本地的 Markdown + Python 脚本组合：
- **无网络服务**、**无密钥存储**、**无外部调用**；
- 运行时数据（`data/`、`growth/`、`*.log`、`config.local.*`）已被 `.gitignore` 排除，不会进入版本库。

若发现脚本存在任意文件读/写、命令注入，或会泄露 `config.local.*` 本地配置的路径穿越类问题，请按上述通道报告。一般的功能 bug 请直接开 issue。
