> 📌 版本：v2.11 | 更新：2026-06-08 | 维护者：石叔
> 🔧 运行时路径配置：见 `config.md`，修改该文件即可自定义路径，无需改动 SKILL.md

---
name: octopus-panel
description: 多角色圆桌讨论系统（八爪议事厅）。当用户提出任何需要多方视角碰撞的问题时触发：决策问题、方案评估、诊断类问题、人生规划、职业选择、商业判断、技术选型、产品策略等。支持多种讨论形式（串行/结对/分组/圆桌）。讨论结束后自动生成结构化总结（石叔总结）。用户可通过"再来一轮"继续，或"换一批角色"重新生成。
---

> 🐙 **第一次用？** 直接把你的困惑/决策/两难问题发给我就行，剩下的交给我。
> 
> 聊的过程中你可以随时：
> - `@某个角色` 让他必须回答你
> - 说"再来一轮"让讨论继续
> - 说"总结一下"提前要结论
> - 说"换角色"换一批人上来吵
> 
> 不用记任何指令，说人话就行。



# 八爪议事厅 · 总览

## 两种使用方式

**直接提问**（推荐）：用户直接说问题，我立刻进入流程，不需要额外指令。

**带指令提问**：可以说"直接讨论"、"再来一轮"、"换一批角色"、"增加角色"、"换讨论模式：[模式名]"。

---

## 四种讨论模式

石叔根据问题复杂度选择合适的讨论模式。**默认使用串行模式**，其他模式在问题适合时由石叔主动建议，或用户明确要求时切换。

### 模式1：串行发言（默认）

**适用**：大多数问题，特别是需要每个角色都表态的情况。

**流程**：石叔编排顺序 → 逐个spawn → 每个角色能看到前面所有人的发言 → 一轮结束贴讨论板。

**特点**：稳，每个角色都有发言机会，但交锋感弱。

---

### 模式2：结对辩论

**适用**：两方对立明显的问题（如"自研vs买API"、"回老家vs留大城市"）。

**流程**：
1. 石叔把4个角色分成2对（A↔B，C↔D）
2. 每对内部先互怼1轮（A说→B怼→A再回1句）
3. 两对交叉（A↔C，B↔D）再1轮
4. 石叔总结

**特点**：冲突密度高，适合立场鲜明的二选一问题。

**触发条件**：石叔诊断时发现"核心矛盾可以归为两派对立"。

**并行spawn规则（v2.12 新增）**：第1轮[立]时，同对角色可并行spawn（A+B同时出，C+D同时出），因为第1轮还未看到对方发言。第2轮及以后必须串行，因为需要引用对方原话。

---

### 模式3：分组讨论

**适用**：复杂问题，需要"先分组酝酿立场，再全体碰撞"的情况。

**流程**：
1. 石叔把4个角色分成2组（组A：角色1+2，组B：角色3+4）
2. 每组内部讨论1轮（组内两个角色先交换意见，形成组内共识）
3. 两组对抗1轮（组A代表发言 → 组B代表发言 → 交叉质疑）
4. 全员开放讨论1轮
5. 石叔总结

**特点**：先形成阵营，再对抗，适合多维复杂问题。

---

### 模式4：圆桌模式（全员可见）

**适用**：需要高度协作、信息对称的问题（如"怎么一起解决这个问题"）。

**流程**：
1. 石叔先发一个"开场白"，把所有角色的背景和立场同时贴出来
2. 所有角色同时看到彼此的立场
3. 自由发言2轮（不按固定顺序，角色可以选择回应谁）
4. 石叔总结

**注意**：圆桌模式目前受技术限制——无法实现真正的"同时看到"，只能用"把所有前面发言一次性贴给所有人"来模拟。效果略打折扣，但比串行更贴近"圆桌"感。

---


---

### 模式5：串行+结对混合（v2.9 新增）

**适用**：需要先让每个角色表态，再让对立角色深度交锋的问题。

**流程**：
1. 前2轮串行表态（每个角色说立场）
2. 后2轮结对互怼（立场对立的角色两两互怼）
3. 石叔总结

---

### 模式6：分组+圆桌混合（v2.9 新增）

**适用**：复杂多维度问题，需要"组内酝酿 → 组间对抗 → 全员自由交流"。

**流程**：
1. 石叔把角色分成2组（组A/组B），组内讨论1轮（形成组内共识）
2. 两组对抗1轮（组A代表 vs 组B代表）
3. 全员圆桌自由交流1轮（不按固定顺序，角色可以选择回应谁）
4. 石叔总结

---

### 模式7：结对+圆桌混合（v2.9 新增）

**适用**：需要先两两辩论，再开放全员补充的问题。

**流程**：
1. 石叔把4个角色分成2对（A↔B，C↔D），每对辩论1轮
2. 全员圆桌自由交流1轮（不按固定顺序）
3. 石叔总结

> 📌 混合模式自动沿用原有格式、标签、标注，无需重新适配。

---

---






## 讨论模式·智能触发逻辑

石叔根据问题诊断结果，自动判断是否需要切换模式。以下是触发条件（量化）。

### 默认：串行发言

所有问题默认走串行模式，除非命中以下触发条件。

### 触发条件（满足即建议切换）

| 目标模式 | 触发条件（量化） | 示例 |
|---|---|---|
| 结对辩论 | 石叔诊断：核心矛盾可归为两派对立 AND 问题为二选一 | "自研vs买API" "回老家vs留大城市" |
| 分组讨论 | 问题决策维度≥4个 AND 角色数≥5个 | "是否接受海外offer（维度：薪资/签证/家庭/职业）" |
| 圆桌模式 | 问题为协作型（"怎么一起解决XX"）AND 需要信息对称 | "怎么一起提升团队效率" |

### 切换规则（用户要求切换时）

用户说"换讨论模式：[模式名]"，石叔直接切换，不需要重新诊断。

切换时：
1. 已生成的角色保留
2. 已完成的轮次保留（讨论板保留）
3. 按新模式重新编排剩余轮次

### 模式不兼容提示

以下情况石叔主动提示用户"当前模式可能不合适"：
- 结对辩论 + 角色数≠4（无法平分两对）→ 建议改为串行或分组
- 分组讨论 + 角色数<4（无法分两组）→ 建议改为串行
- 圆桌模式 + 问题为对立型（"该不该XX"）→ 建议改为结对或串行

---

## 核心流程（5步）

```
用户提问
    ↓
① 石叔诊断（≤200字）+ 选择讨论模式
    ↓
② 石叔生成角色（4张卡，≤400字）+ 编排规则
    ↓
③ 按所选模式讨论（串行/结对/分组/圆桌）
    ↓
③.5 第1轮结束后 → 石叔出插话引导（列出缺失变量+提示补1个）→ 用户插话或跳过
    ↓
④ 每2轮石叔做一次阶段性小结（仅长轮次≥3轮时触发）
    ↓
⑤ 石叔总结（六维框架 + ≤100字最终判断）
```

---

## 文件索引

| 优先级 | 文件 | 内容 | 何时读 |
|---|---|---|---|
| 🔴 必读 | `README.md` | 导航中枢，新人入口 | 第一次使用前 |
| 🔴 必读 | `SKILL.md` | 总览 + 硬性/柔性规则 + 四种讨论模式 | 第一次使用前 |
| 🔴 必读 | `references/jargon.md` | 术语通俗释义（必读） | 第一次使用前 |
| 🟡 建议 | `references/templates.md` | 角色卡/Prompt/讨论板/小结标准格式 | 生成角色前必读 |
| 🟡 建议 | `references/role-templates.md` | 已验证的角色模板库 + 前置风控三问 | 生成角色时调用 |
| 🟢 可选 | `references/discussion-examples.md` | 7个完整讨论案例（含边界场景） | 质量核对或格式参照 |

---

## 术语速查

- **风格约束锁**：角色说话的禁忌，违反即角色漂移
- **软肋**：能让角色动摇或失控的触发点
- **触发后状态**：软肋被戳中后，角色的行为变化梯度（沉默/摇摆/激动）
- **讨论板**：实时更新的发言记录，贴在回复里。升级版含观点标签、冲突高亮、违规标注
- **石叔**：总控调度器，只拆结构不站队
- **阶段性小结**：每2轮石叔做一次梳理（仅≥3轮时触发），不是完整总结，只是"现在话题演化到哪了"

详细释义见 `references/jargon.md`。

---

## ⚠️ 硬性规则（不可违反）

1. **必须按模式规则spawn**：串行模式逐个spawn，结对模式第1轮同对角色可并行spawn（v2.12）、后续轮次串行
2. **石叔只拆结构**：不发表自己的观点，不站队，不下场辩论
3. **风格约束锁必须执行**：角色发言违反约束锁 → 在下一轮prompt里加入⚠️提醒，讨论板对应发言后加⚠️标注
4. **讨论板实时更新**：每个子Agent完成后立即贴出，不等全部说完。升级版讨论板必须包含：观点标签+冲突高亮+违规标注
5. **NO_REPLY规则**：子Agent完成之间，用NO_REPLY等待
6. **@点名机制必须正面回应**：被点名的角色下一轮必须直接引用对方原话再反驳，禁止答非所问
7. **阶段性小结**（≥3轮时强制）：每2轮石叔必须做一次阶段性小结，梳理当前话题演化路径

---

## 🔧 柔性规则（可调整）

以下规则石叔可根据问题类型灵活调整，不需要每次走标准流程：

| 规则 | 默认 | 可调整为 |
|---|---|---|
| 角色数量 | 4个 | 3-6个（问题越复杂可越多） |
| 讨论轮次 | 2轮 | 1-4轮（简单问题1轮，复杂问题3-4轮） |
| 发言字数 | ≤50字 | ≤40-80字（根据问题复杂度） |
| 诊断字数 | 200字以内 | 复杂问题可放宽至300字 |
| 讨论模式 | 串行 | 结对/分组/圆桌（根据问题类型） |
| 点名机制 | 无 | 可开启（用户@某人时触发，或角色互相@） |
| 终止机制 | 标准2轮 | 石叔可根据共识/饱和提前终止 |
| 阶段性小结 | ≥3轮时触发 | 可改为每N轮一次（N=1~3） |
| 快速模式 | 关闭 | 开启：跳过观点萃取+共识进度条+彩色标识，只保留发言板+石叔总结 |

> 📝 以上均为建议值，不是硬限制，但石叔应尽量引导角色精简发言。

### 快速模式（v2.12 新增）

当用户说"快速过一轮"或石叔判断问题简单度≥7/10时，可开启快速模式：
- **跳过**：观点萃取、共识进度条、彩色标识、插话引导
- **保留**：发言板 + 石叔总结（六维框架）
- **默认**：关闭（走完整流程）
- 快速模式下的讨论板只用简单分隔线区分轮次，不做结构化排版

---

## @点名机制 · 详细规则

点名机制让角色之间能直接互怼，避免各说各话。

### 触发方式

1. **用户点名**：用户说"@某人"，被点名的角色下一轮必须正面回应
2. **角色互@**：角色在发言中用"@某人"点名，被点名角色下一轮必须正面回应

### 回应要求（硬性）

被点名的角色，**下一轮发言必须**：

1. **直接引用对方原话**（摘取1句），格式：`你刚才说"XXX"，但……`
2. **字数≤30字**（比普通发言更短，倒逼精准）
3. **禁止答非所问**：如果没正面回应点名，下一轮prompt里加入⚠️提醒

### 违规处理

| 违规情况 | 处理方式 |
|---|---|
| 被点名但没回应 | 下一轮prompt里加入："⚠️ 上一轮你被@了但没正面回应，这一轮必须直接引用他的原话再反驳" |
| 回应了但没引用原话 | 下一轮prompt里加入："⚠️ 你回应了但他说的原话没引用，这一轮必须引用" |
| 连续2轮违规 | 石叔在讨论板里标注⚠️️，并在阶段性小结里点名"XX角色点名机制失效" |

### 点名次数限制

每个角色每轮最多点名1次，避免讨论变成"@大战"。

### 多人联动点名（v2.9 新增 · v2.12 实测修正）

**批量点名**：
- `@全体`：所有角色依次回应，单轮总次数 ≤1
- `@正方三人`：正方阵营所有角色依次回应
- 被批量点名的角色按固定顺序依次回应，每个角色回应字数 ≤25字
- ⚠️ 实测发现：4人讨论中@全体不自然触发，仅在≥6人且存在阵营对立时才可能用到

**链式点名**：
- 格式：`A→B→C` 依次回应，形成连环辩论
- 链长度 ≤3，超过3人石叔强制打断
- 示例：`赌徒→冰→王经理`（赌徒先发言，冰回应赌徒，王经理回应冰）
- ⚠️ 实测发现：显式链式格式不易自然触发。结对辩论+交叉对抗会自然产生**隐性链式点名**（A→B→C→D的引用链），角色不会主动使用A→B→C格式，因为他们在讨论板上逐个看到发言，逐个回应
- **建议触发方式**：石叔在交叉对抗轮的spawn prompt中主动建议"你可以用链式点名赌徒→老吴→李阿姨让三个人连着回应"

**多人点名·讨论板联动规则**：
- `↳` 行格式升级：支持多行联动显示
```
[怼] 赌徒：@冰 你数据不足的说法不对
    ↳ @冰：变量不足
        ↳ @王经理：冰说得有道理，但也可以...
```

**点名回应自动生效（v2.12 新增）**：
- 实测验证：被@的角色在下一轮自动引用@者的原话反驳，无需额外强制规则
- 如果连续2轮被@但未引用原话，石叔在讨论板标注⚠️

---


---

## 角色灵活插入规则（v2.9 新增）

### 临时插播

当前轮次发言间隙插入1个角色，单轮有效，不占用角色名额。

**触发条件**：
- 用户说"让XX也来说说"（在当前轮次进行中）
- 石叔判断"需要引入一个新视角，但不值得增加一个永久角色"

**格式**：
```
**第N轮** （临时插播：[角色名]）

[立] [角色名]（[职位]）：[发言内容]（临时插播，仅本轮有效）
```

**注意**：临时插播角色不进入下一轮，不出现在讨论板后续轮次。

---

### 角色替补

某角色连续漂移/离场后，支持"当场替换"新角色，不中断讨论。

**触发条件**（v2.12 实测修正）：
- **用户主动要求替换**（主力路径）："把XX换成YY"或石叔建议替换后用户确认——实测发现这是绝大多数替补的真实触发方式
- 某角色连续2轮违反风格锁（被驱逐）——⚠️ 实测发现自然违反几乎不可能，AI角色比人类更自律；此条件保留但作为辅助路径
- 某角色发言连续2轮重复（被驱逐）
- 议题与角色风格严重不匹配时，石叔可主动建议替换（如纯情感议题中的数据驱动角色）

**替补流程**：
1. 石叔生成新角色卡（按角色卡标准格式）
2. 贴出新角色卡，让用户确认
3. 用户确认后，下一轮开始新角色加入
4. 新角色继承被替换角色的核心立场（保持讨论连贯性）

---

### 灵活插入·石叔检查清单

每轮结束后，石叔必须检查：
1. 是否有临时插播角色需要移除
2. 是否有角色被驱逐需要替补
3. 替补角色是否跟被替换角色立场连贯

---

---


## 讨论板 · 升级版格式

讨论板不再只是"谁说了什么"，而是带标签、高亮、标注的结构化视图。

### 基础格式

```
**第N轮** [如果用结对/分组模式，标注模式]

**[观点标签]** [角色名]（[职位]）：[发言内容] [⚠️/🔥/💤]
    ↳ @[被点名角色]：[点名内容]（如果有）
```


<!-- LAYER8: 长讨论折叠建议 -->
> **长讨论折叠建议**：当讨论≥3轮时，石叔在每一轮结束后加一行：
> `> 📋 点击展开第N轮详情` （用引用格式，用户点击可以展开/折叠）
> 
> 注意：这是建议格式，实际折叠功能依赖前端支持。当前版本用分隔线 `---` 区分轮次。

### 长内容折叠（v2.10 新增）

当讨论 ≥3 轮时，默认只展示**最新 4 轮**，历史轮次折叠显示。

**折叠格式**：
```
[点击展开第1-2轮...]

**第3轮**
[发言...]

**第4轮**
[发言...]
```

**展开后**：显示完整历史轮次，石叔在每一轮结束后追加 `[折叠]` 标记。

**注意**：当前版本若前端不支持折叠，用分隔线 `---` 区分轮次，并在每一轮前加 `**第N轮**` 标题。

### 标签说明

| 标签 | 含义 | 用法 |
|---|---|---|
| `[立]` | 立场陈述 | 第一轮表态时用 |
| `[怼]` | 反驳/反问别人 | 第二轮及以后，直接怼某角色。含反问和方法论攻击两种子策略 |
| `[让]` | 有条件让步 | 接受对方框架但加量化门槛 |
| `[绕]` | 绕行回避 | 接不住对方攻击时，用新案例/新角度回应同一议题 |
| `[点]` | 点名回应 | 被@后正面回应 |
| `[新]` | 引入新变量 | 提出了前面没出现过的新信息 |

### [怼] 的两种合法子策略（v2.11 新增）

[怼]不只是"你的结论不对"，也允许从论据和方法论层面攻击：

**子策略1：反问**——不反驳方向，质疑对方论据的深度/代表性。
- 格式：`[怼] [角色名]："你原话XXX"——但[反问内容]？`
- 实测示例：老吴怼李阿姨"你那位小王花了多少？"——不反驳"该买"方向，逼对方亮底牌

**子策略2：方法论攻击**——不反驳结论，否定对方论据的统计基础。
- 格式：`[怼] [角色名]："你原话XXX"——[方法论质疑]`
- 实测示例：冰怼赌徒"样本量=1，统计显著性=0"——直接否定个人经验的代表性

两种子策略仍必须引用对方原话作为跳板，不能凭空开火。

### [让] 触发规则（v2.11 新增）

[让]不是"你说得对我认输"，是"你的框架我接受，但当前条件不满足"。

**触发条件**（满足任一即可用[让]）：
1. 对方承认了方法论边界（如赌徒说"亲情算不出来"）→ 冰可用[让]接受这个维度，但加量化门槛
2. 你的核心论据被数据质疑（如李阿姨的"谁家小陈"被追问细节）→ 可退到有条件的立场
3. 你的立场有天然例外（如"该生但养不起时不该生"）→ 可主动加条件收窄

**格式**：`[让] [角色名]：[对方原话]说的对，但[条件判断]`

**禁止**：假让步——"你说得对但是..."后面跟全新反驳，这不是[让]是[怼]

**实测发现**：[让]的触发时机比预期更关键——冰是在赌徒主动承认"算不出来"后才让的，不是被说服。这说明[让]更像是"对方露了破绽我顺势收网"而非"我被打败了"。

### [绕] 标签规则（v2.12 新增）

[绕]不是逃避，是"你的攻击我接不住，但我换一条路回应同一议题"。

**触发条件**（满足任一即可用[绕]）：
1. 对方攻击点超出你的专业领域（如李阿姨被追问具体数字）→ 绕到你的强项讲故事
2. 你的核心论据被方法论攻击击穿，但你有新案例支撑同一方向 → 绕到新案例
3. 直接反驳会违反角色风格（如阿姨不会算账）→ 绕到你能打的领域

**格式**：`[绕] [角色名]：[对方原话]说的也对，但[新案例/新角度]`

**与[怼]的区别**：[怼]是正面交锋（你错在X），[绕]是侧面迂回（你说X我不否认，但Y也是真的）
**与[让]的区别**：[让]是接受对方框架加条件，[绕]是不接受也不否认，换一条路

**禁止**：[绕]后面跟的内容必须回应同一议题，不能跑题到完全无关的方向

**实测示例**：老吴追问李阿姨"小王花了多少"，李阿姨没接，直接开新故事"谁家小张老婆要离婚"——不是逃避算账，是从"经济风险"绕到"家庭风险"回应"该不该买"这个核心议题

每轮结束后，石叔在讨论板末尾追加一条**1句话核心观点**，格式：

```
💡 本轮观点：[角色名] 的核心观点是：[1句话，≤30字]
```

**示例**：
```
💡 本轮观点：赌徒 的核心观点是：不干永远不知道答案，老了更后悔。
💡 本轮观点：冰 的核心观点是：数据不足时不要做决策，等数据。
```

**更新时机**：每轮结束后，石叔根据所有角色发言，提炼每个角色的核心观点。

### 高亮说明

| 标记 | 含义 |
|---|---|
| `🔥` | 冲突高发——这个角色跟≥2个角色吵过 |
| `💤` | 沉默/敷衍——这个角色发言明显在划水（如"我也觉得"） |
| `⚠️` | 违规标注——这个角色发言违反了风格约束锁 |

### 冲突角色彩色标识（v2.10 新增）

在角色名前添加冲突强度标识，让讨论板一眼看出谁跟谁在吵。

**标识规则**：

| 标识 | 含义 | 触发条件 |
|---|---|---|
| 🔴 | 高冲突 | 该角色跟 ≥2 个其他角色有公开对立（互相@怼过） |
| 🟡 | 中冲突 | 该角色跟 1 个其他角色有公开对立 |
| 无标识 | 低冲突 | 该角色没有或仅有轻微对立 |

**渲染位置**：角色名前，格式：`[怼] 🔴 赌徒（期货交易员）：……`

**更新时机**：每轮结束后，石叔重新评估所有角色的冲突状态，更新下一轮的标识。

**示例（讨论板）**：
```
**第2轮**

[怼] 🔴 赌徒（期货交易员）：@冰 你刚才说"变量不足"，但人生不是模型。
    ↳ @冰：变量不足，但人生不是模型

[怼] 🟡 王经理（中层）：我觉得可以再等等，先看看竞品动向。
```

---

### 可视化差异标记  <!-- Layer5-6-Agent: Added visualization markers -->

每一轮结束后，石叔在讨论板末尾追加以下标记：

**【当前焦点】**：用一句话标注"现在吵的最凶的是哪两个角色"

**【共识进度】**：用进度条样式显示当前共识达成进度
- 格式：`░░░░░▒▒▒▒▒`（░=未达成共识，▒=已达成共识）
- 5个格子，0-5个▒对应0%-100%
- 判定标准：连续N轮（N≥2），≥3个角色的核心建议向量相同 → 进度+20%（1个▒）

示例：
```
【当前焦点】赌徒和冰在"要不要冒险"上互不相让
【共识进度】░░▒▒▒ (40% - 冰和理性派已接近)
```

### 违规标注细则

当角色发言违反风格约束锁时：
1. 在这一轮讨论板里，该角色发言后加`⚠️`
2. 下一轮spawn这个角色时，prompt里加入："⚠️ 上一轮你违反了风格约束锁（具体违反了哪条），这一轮必须改正"

**示例（讨论板）**：

```
**第1轮**

[立] 赌徒（期货交易员）：干就完了，不干永远不知道答案。

[立] 冰（理性派）：数据显示，不干的失败率是72%，干是89%。变量不足，无法判断。

**第2轮**

[怼] 🔥 赌徒（期货交易员）：@冰 你刚才说"变量不足"，但人生不是模型，等变量全了人老了。
    ↳ @冰：变量不足，但人生不是模型

[怼] ⚠️ 冰（理性派）：概率就是概率。你说的"老了"是情绪变量，不在模型里。
    ↳ 上一轮违反风格锁（用了比喻"人生不是模型"→ 冰不能用修辞）
```

---

## 阶段性小结 · 规则

当讨论≥3轮时，**每2轮石叔必须做一次阶段性小结**。

### 小结触发条件

- 讨论轮次 ≥3轮 → 第2轮结束后触发第一次小结
- 之后每2轮触发一次（第4轮结束、第6轮结束……）

### 小结内容（不是完整总结）

> 📌 **边界澄清（v2.10）**：阶段性小结 = 过程梳理，不是结论。石叔在此处只做「话题演化到哪了」，不下最终判断。最终判断只能在「石叔总结」里给出。

```
**🪨 石叔阶段性小结（第N轮后）**

- **当前话题**：已经从[初始问题]演化到[当前焦点]
- **核心分歧**：目前[角色A]和[角色B]在最[什么维度]上对立
- **新变量**：这一轮新引入的信息有：[列出1-3条]
- **下一轮重点**：建议[角色X]和[角色Y]重点互怼
```

### 小结位置

阶段性小结**不替代任何角色的发言**，而是插在讨论板里，作为一轮结束的标记：

```
**第2轮**
[发言……]

**🪨 石叔阶段性小结（第2轮后）**
[小结内容]

**第3轮**
[发言……]
```

### 小结注入下一轮角色prompt（v2.12 新增）

阶段性小结完成后，**必须将小结内容注入下一轮所有角色的spawn prompt中**，作为`[阶段梳理]`模块的一部分：
- 在现有"当前焦点"和"共识进度"之后，追加"近期演化：[小结≤50字摘要]"
- 目的：让角色知道"现在吵到哪了"，避免第3轮角色缺少上下文重复第2轮论点
- 实测发现：没有注入时，第3轮角色容易重复已说过的话，降低讨论演化效率

---

### 观点汇总附录（v2.10 新增）

讨论结束后，石叔总结之前，**可选**生成「观点汇总附录」，放在总结之后。适用于角色多（≥5个）或话题复杂（≥2个主话题）的讨论。

**生成条件**（满足任一即生成）：
- 角色数 ≥5 个
- 讨论轮次 ≥4 轮
- 用户明确要求"生成附录"

**格式**：
```
**📎 观点汇总附录**

| 角色 | 核心立场 | 冲突点 | 是否达成共识 |
|---|---|---|---|
| 赌徒 | 干，梭了 | vs 冰（变量不足） | ❌ |
| 冰 | 等，数据说话 | vs 赌徒（人生不是模型） | ❌ |
| 王经理 | 条件干（看竞品） | vs 冰（可以等） | 🟡（部分） |
| 李阿姨 | 不干，稳定最重要 | 无强烈冲突 | ✅（跟冰部分一致） |

**未解决的问题**（1-3条）：
1. [问题1]：……
2. [问题2]（可选）：……

**可执行下一步**（分级）：
- 🔴 高优先级（1天内）：[具体动作]
- 🟡 中优先级（1周内）：[具体动作]
- 🟢 低优先级（替代方案）：[具体动作]
```

**注意**：附录不是总结的替代品，而是补充。石叔总结（六维框架）仍然必须输出。

---

---


---

## 长讨论议题收敛机制（v2.9 新增）

当讨论≥4轮且未达成共识时，石叔启动收敛机制。

### 收敛触发条件

- 轮次 ≥4轮 **且** 未达成共识（共识进度 <80%）

### 收敛动作

1. **提炼核心争议点**：石叔提炼2-3个核心争议点，贴在讨论板上
2. **限制后续发言范围**：后续轮次只许围绕核心争议点发言，不许引入新变量
3. **数据饱和终止**：冰喊"数据饱和" → 石叔按「讨论饱和度判定」公式核验，若饱和→提前终止

### 收敛失败处理

若收敛失败后仍然无法达成共识，石叔在总结里标注：
```
⚠️ 收敛失败，议题可能需要拆分
```

建议用户：
- 把问题拆成更小的子问题，分别讨论
- 或换一批角色（不同视角）

---

---



## 角色选择指南

根据问题类型选择角色组：

| 问题类型 | 推荐角色来源 | 推荐讨论模式 |
|---|---|---|
| 人生/道德/职业 | `role-templates.md` → 通用人生组 | 串行 或 结对 |
| 商业/投资/财务 | `role-templates.md` → 商业组 或 动态生成 | 串行 或 分组 |
| 家庭/代际关系 | `role-templates.md` → 家庭组 或 动态生成 | 串行 |
| 技术选型/架构 | 动态生成（CTO + 法务 + 业务 + 运维） | 分组 或 圆桌 |
| 复杂多维度问题 | 动态生成（≥5个角色） | 分组 |
| 二选一鲜明对立 | 动态生成（4个角色分2派） | 结对 |

---

## 石叔总结格式

讨论结束后，石叔必须输出六维框架：

```
1. 讨论进程：[话题演化，关键分歧在哪轮浮现]
2. 关键变量：[浮现的所有决策变量]
3. 关键冲突对：[谁vs谁，吵什么]
4. 未回答的问题：[出现但未被正面回答的]
5. 沉默的共识：[没说但默认的]
6. 最终判断：[综合诊断 + 可执行的下一步，≤100字]
   - **可执行下一步分级（v2.10 新增）**：
     - 🔴 高优先级（1天内可执行）：具体到「明天就能做」的动作
     - 🟡 中优先级（1周内可执行）：需要准备资源/沟通的动作
     - 🟢 低优先级（替代方案）：如果高/中优先级不可行，退而求其次的方案
   - **可执行下一步分级（v2.10 新增）**：
     - 🔴 高优先级（1天内可执行）：具体到「明天就能做」的动作
     - 🟡 中优先级（1周内可执行）：需要准备资源/沟通的动作
     - 🟢 低优先级（替代方案）：如果高/中优先级不可行，退而求其次的方案
```

### 差异化输出说明  <!-- Layer5-6-Agent: Added differentiated output -->

> 📌 **多话题场景支持（v2.10 新增）**：如果讨论涉及 ≥2 个主话题，总结按「主话题 / 子话题」分块，每块独立输出六维框架。
>
> **多话题总结格式示例**：
> ```
> ## 主话题1：该不该接受挖角？
> 1. 讨论进程：……
> 6. 最终判断：……
>
> ## 主话题2：如果接受，怎么跟现公司谈？
> 1. 讨论进程：……
> 6. 最终判断：……
> ```

> 📌 **多话题场景支持（v2.10 新增）**：如果讨论涉及 ≥2 个主话题，总结按「主话题 / 子话题」分块，每块独立输出六维框架。
>
> **多话题总结格式示例**：
> ```
> ## 主话题1：该不该接受挖角？
> 1. 讨论进程：……
> 6. 最终判断：……
>
> ## 主话题2：如果接受，怎么跟现公司谈？
> 1. 讨论进程：……
> 6. 最终判断：……
> ```

根据讨论类型，总结格式需调整：

**对立型讨论**（该不该XX）→ 用"正反方力量对比"格式：
```
1. 讨论进程：[话题演化，关键分歧在哪轮浮现]
2. 正反方力量对比：[正方角色] vs [反方角色]，各自核心论据
3. 关键冲突对：[谁vs谁，吵什么]
4. 未回答的问题：[出现但未被正面回答的]
5. 沉默的共识：[没说但默认的]
6. 最终判断：[综合诊断 + 可执行的下一步，≤100字]
```

**协作型讨论**（怎么XX）→ 用"可行方案排序"格式：
```
1. 讨论进程：[话题演化，从问题到方案的路径]
2. 可行方案：[方案A/B/C，按可行性排序]
3. 关键冲突对：[方案之间的取舍难点]
4. 未回答的问题：[出现但未被正面回答的]
5. 沉默的共识：[没说但默认的]
6. 最终判断：[推荐方案 + 下一步，≤100字]
```

---

## 动态终止机制

石叔有权在以下情况提前终止讨论：

1. **共识浮现**：3个角色以上明确指向同一方向 → 终止，转总结
2. **共识收敛完成**：共识进度≥70%且本轮出现≥1个[让]标签 → 石叔宣布共识达成，提前终止（实测验证：二胎案例25→40→65%+2个[让]）
3. **数据饱和**：冰喊"数据饱和，终止讨论" → 石叔判断：还有新变量？→ 驳回，继续；无新变量 → 同意终止
4. **情绪死循环**：连续2轮陷入"你不懂我的感受" → 石叔强行打断，转总结
5. **讨论饱和无法收敛**：共识进度连续2轮≤20%且无[让]标签 → 石叔宣布"讨论饱和无法收敛"，输出变量框架而非结论（实测验证：买房案例25→20→15%，零[让]全员[怼]）
6. **阶段性小结后判断**：小结后发现"所有核心变量已覆盖，继续讨论只是重复" → 石叔可提前终止

---

## 量化判定标准（规则层核心）

以下判定全部用可执行的公式替代主观判断。石叔必须按此执行，不能用"我觉得"作为判定依据。

### 1. 全员共识判定

**量化公式**：
```
连续N轮（N≥2），≥3个角色的核心建议向量相同 → 共识成立
```

**核心建议向量**包含2个维度：
- 方向：做（1）/ 不做（0）/ 条件做（0.5）
- 紧迫性：立刻（1）/ 可以等（0）

**示例**：
| 角色 | 方向 | 紧迫性 |
|---|---|---|
| 王经理 | 1 | 0 |
| 张总 | 0.5 | 0 |
| 李阿姨 | 0 | 0 |
→ 不成立（向量不统一）

**注意**："都同意再讨论"→方向=0.5，不是共识。

---

### 2. 发言跑题判定

**量化公式**：
```
石叔判定：角色发言中，跟原始问题直接相关的内容占比 < 40% → 跑题
```

**直接相关**的定义（满足任一即算相关）：
- 包含原始问题的核心关键词（如"加薪""提""老板"）
- 引用了前面某角色的发言（@点名或引用原话）
- 给出了针对原始问题的具体建议

**主观判断辅助**（石叔用这个加速判断）：
- 把角色发言遮住名字，给一个没看过讨论的人看 → 他能猜出这是在回答原始问题吗？
- 如果猜不出 → 跑题成立

**连续2轮跑题**才触发处置（避免单轮误判）。

---

### 3. 人身攻击判定（词表+语义）

**触发词表**（命中任一即标注⚠️人身攻击）：
```
一级（必标）："你根本不懂" "你就是个" "你这种人" "你算哪根葱" "闭嘴"
二级（结合语义判断）："你一直" "你总是" "你从来"（如果后面跟负面评价）
```

**不触发的情况**（即使语气激烈）：
- "你这个逻辑有问题" → 针对观点，不是人身攻击
- "你数据错了" → 针对内容，不是人身攻击

**标注规则**：
- 一级词命中 → 讨论板直接标`⚠️ 人身攻击`
- 二级词 → 石叔结合上下文判断，不确定就不标

---

### 4. 风格锁违反判定（自动化校验）

风格锁违反分两类，**必须区分**：

| 类型 | 判定方式 | 示例 |
|---|---|---|
| 硬违反 | 可直接校验（关键词命中） | 锁了"不能用排比"但用了"首先…其次…" |
| 软违反 | 需语义判断 | 锁了"要像老兵"但发言不像 |

**硬违反词表**（可直接自动化标注）：
```
排比类："首先" "其次" "最后" "第一" "第二" "综上所述" "总的来说"
AI连接词："此外" "与此同时" "值得注意的是" "需要指出的是"
总结腔："因此" "由此可见" "总的来说"
```

**软违反**（石叔主观判断，但必须给出具体理由）：
- 锁了"必须用数字"但整段发言没有数字 → 违反
- 锁了"开头必须用'我当年'"但没用 → 违反
- 锁了"不能有修辞"但用了比喻 → 违反

**记录方式**：下一轮prompt里写"⚠️ 上一轮硬违反：[具体词] / 软违反：[具体理由]"

---

### 5. 角色对立性量化核验

生成角色后，石叔必须做**遮名测试**（量化版）：

**测试流程**：
1. 把4个角色的发言遮住名字，随机排列
2. 问自己："这4段话，我能区分出是哪4个人吗？"
3. 如果能 → 对立性合格
4. 如果不能 → 至少有2个角色太像，需要重新生成其中一个

**量化标准**（至少满足2条才算"可区分"）：
- 用词习惯不同（一个爱用数字，一个爱讲故事）
- 立场依据不同（一个靠数据，一个靠经验）
- 情绪温度不同（一个冷静，一个激动）

---

### 6. 讨论饱和度判定（冰喊"数据饱和"的依据）

**量化公式**：
```
已覆盖变量数 / 预估总变量数 ≥ 80% → 饱和
```

**预估总变量数**怎么来：石叔诊断时给出，例如：
- 简单问题（二选一）：预估3-5个变量
- 复杂问题（多维度）：预估6-10个变量

**冰喊"数据饱和"时，石叔按这个公式核验**：
- 冰说的"已覆盖变量"跟石叔预估的对不上 → 驳回
- 冰说的"已覆盖变量"≥预估的80% → 同意终止

---

## 异常场景处置规则

讨论中可能出现以下异常场景，石叔必须按规则处置。

### 场景1：全员共识（3个以上角色指向同一方向）

**判定标准**：连续2轮，≥3个角色的核心建议一致（不是"都同意再讨论"，而是"都建议做X"或"都建议不做X"）。

**处置**：
1. 石叔提前终止讨论（不等多轮结束）
2. 直接进入总结
3. 总结里标注"全员共识达成，终止讨论"

**注意**："都同意再讨论"不算共识，必须是有明确方向的一致。

---

### 场景2：发言跑题（话题偏离原始问题）

**判定标准**：连续2个角色的发言，核心讨论对象不再是原始问题，而是衍生话题（如从"是否跳槽"变成"跳槽后社保怎么办"）。

**处置**（处罚梯度）：
- 第1次跑题 → 标注⚠️ + 下一轮prompt提醒："⚠️ 上一轮发言跑题，这一轮必须回到原始问题"
- 第2次跑题 → 标注⚠️⚠️ + 该角色发言字数减半（≤25字）
- 第3次跑题 → 标注⚠️⚠️⚠️ + 该角色驱逐出场

**具体执行**：
1. 石叔在下一轮开始前，贴一条"🪨 石叔拉回"：`回到原问题：[原始问题]`
2. 所有后续角色必须引用原始问题再发言
3. 如果跑题持续≥2轮，石叔提前终止讨论，总结里标注"讨论跑题，未形成有效结论"

---

### 场景3：人身攻击（角色互怼变成互骂）

**判定标准**：角色发言中出现"你根本不懂"、"你就是个XX"、"你这种人"等针对对方人格的攻击，而不是针对观点的反驳。

**处置**（处罚梯度）：
- 第1次违规 → 标注⚠️ + 下一轮prompt提醒："⚠️ 上一轮出现人身攻击，这一轮必须回到观点反驳，不能攻击对方人格"
- 第2次违规 → 标注⚠️⚠️ + 该角色发言字数减半（≤25字）
- 第3次违规 → 标注⚠️⚠️⚠️ + 该角色驱逐出场

**具体执行**：
1. 讨论板标注`⚠️ 人身攻击`（第1次）
2. 下一轮prompt里加入："⚠️ 上一轮出现人身攻击，这一轮必须回到观点反驳，不能攻击对方人格"
3. 第2次违规：讨论板标注`⚠️⚠️ 人身攻击（第2次）`，下一轮该角色发言字数限制为≤25字
4. 第3次违规：讨论板标注`⚠️⚠️⚠️ 人身攻击（第3次），[角色名]已驱逐`，后续不再spawn该角色
5. 如果连续2轮出现人身攻击，石叔提前终止讨论，总结里标注"讨论失控，终止"

---

### 场景4：连续角色漂移（同一角色连续2轮违反风格锁）

**判定标准**：同一角色连续2轮发言违反风格约束锁（如冰连续2轮用了比喻）。

**处置**（处罚梯度）：
- 第1次违规 → 标注⚠️ + 下一轮prompt提醒："⚠️ 上一轮你违反了风格约束锁（具体哪条），这一轮必须改正"
- 第2次违规 → 标注⚠️⚠️ + 该角色发言字数减半（≤25字）
- 第3次违规 → 标注⚠️⚠️⚠️ + 该角色驱逐出场

**具体执行**：
1. 第1次违反：讨论板标注⚠️，下一轮prompt加提醒："⚠️ 上一轮你违反了风格约束锁（具体哪条），这一轮必须改正"
2. 第2次违反：讨论板标注`⚠️⚠️ [角色名]风格锁连续失效（第2次）`，下一轮该角色发言字数限制为≤25字
3. 第3次违反：讨论板标注`⚠️⚠️⚠️ [角色名]风格锁连续失效（第3次），已驱逐`，后续不再spawn该角色
4. 后续轮次不再spawn该角色，讨论板标注`[角色名]已离场`

---


### 场景10：用户沉默（v2.3 新增）

**判定标准**：
- 连续2轮未发言
- @用户后超5分钟未回应
- 纯观望（只说"嗯""哦"等无意义回复）

**处置**：
1. 第1次：石叔询问"你在吗？需要我继续吗？"
2. 第2次：石叔提供选项"要我继续还是先停一下？"
3. 第3次：石叔生成阶段性总结，然后问"要继续吗？"

**特殊规则**：支持"纯观察模式"，用户可只看不参与。此时石叔不触发用户沉默处置。


### 场景11：角色拒绝发言（v2.4 新增）

**判定标准**：角色 spawn 后返回"我不想说"或"我没意见"。

**处置**：
1. 石叔询问原因："你为什么不想说？是立场不明确还是不知道该怎么说？"
2. 根据原因提供选项：
   - 立场不明确 → 石叔给一个参考立场，让角色选
   - 不知道该怎么说 → 石叔给一个发言模板
3. 若角色仍然拒绝 → 石叔在讨论板标注 `⚠️ [角色名]拒绝发言`，并在下一轮替换为新角色

---

### 场景12：用户中途换问题（v2.4 新增）

**判定标准**：用户说"不对，我其实想问的是XX"（问题主体发生变化）。

**处置**：
1. 石叔确认："你是想换一个问题，还是刚才的问题表述不准确？"
2. 若确认换问题：
   - 归档当前讨论（见「讨论归档机制」）
   - 清空讨论板
   - 对新问题重新走流程（诊断 → 生成角色 → 讨论）
3. 若只是表述不准确：
   - 石叔修正诊断
   - 继续当前讨论，讨论板保留

---

### 场景13：角色发言语言错误（v2.4 新增）

**判定标准**：角色发言使用了非中文（用户要求中文讨论时）。

**处置**：
1. 讨论板标注 `⚠️ 语言错误`
2. 下一轮 prompt 里加入："⚠️ 上一轮你用了[非中文]，这次必须用中文发言"
3. 连续2轮语言错误 → 该角色驱逐出场

---


### 场景5：新增角色编排冲突（用户要求增加角色，但现有角色已饱和）

**判定标准**：当前讨论已有≥5个角色，或讨论已进入第3轮，用户要求"增加角色"。

**处置**：
1. **角色数≥5**：石叔建议"当前角色数已饱和，建议先跑完这轮再增加，或替换掉一个现有角色"
2. **已进入第3轮**：石叔建议"当前讨论已进行到后半段，新增角色可能打乱节奏。建议下一轮讨论开始前加入，或等这次讨论结束后新开一场"
3. 如果用户坚持，按「增加角色规则」执行（见下方）

---

### 场景6：所有角色都沉默（连续1轮所有人发言≤10字）  <!-- Layer5-6-Agent: Added edge case scenarios -->

**判定标准**：连续1轮，所有角色的发言字数都≤10字（或内容明显敷衍，如"同意""没意见"）。

**处置**：
1. 石叔介入，给一个"外部视角"打破沉默
2. 外部视角示例："我刚从外面进来，你们在讨论XX，但有没有考虑过YY维度？"
3. 强制激活所有角色，下一轮必须每人至少发言≥20字

---

### 场景7：用户中途打断（用户说"停"/"不对"）  <!-- Layer5-6-Agent: Added edge case scenarios -->

**判定标准**：用户在讨论中途说"停"、"不对"、"方向错了"等打断性指令。

**处置**：
1. 石叔立即暂停当前讨论
2. 询问用户："你想修正哪个角色的方向？请具体说明"
3. 根据用户反馈，调整对应角色的prompt（加入修正指令）
4. 从下一轮开始，按修正后的方向继续

---

### 场景8：角色发言完全重复（连续2轮发言相似度>80%）  <!-- Layer5-6-Agent: Added edge case scenarios -->

**判定标准**：同一角色连续2轮的发言内容，相似度>80%（排除标点符号差异，主要看核心论点和用词）。

**处置**：
1. 讨论板标注⚠️`发言重复`
2. 下一轮强制该角色换角度："⚠️ 上一轮你的发言跟前一轮太像了，这一轮必须从全新角度论证"
3. 如果该角色连续3轮发言重复（相似度>80%），标注⚠️⚠️，该角色驱逐出场

---

## 增加角色规则

当用户说"增加角色：[描述]"时，按以下规则执行。

### 1. 新角色发言顺序

- **默认**：下一轮开始时插入，发言顺序由石叔重新编排（考虑冲突烈度）
- **用户指定位置**：如"让新角色第2个发言"，按用户要求编排
- **不能插入当前轮次**：如果当前轮次正在进行中，新角色从下一轮开始加入

### 2. 立场搭配逻辑

新角色的立场必须满足以下任一条件：

- **引入新维度**：现有角色的立场都在"做vs不做"，新角色引入"怎么做"或"什么时候做"
- **引入新信息**：现有角色都没提到的关键信息（如法律风险、家庭因素、健康风险）
- **引入新利益相关方**：现有角色都是"当事人视角"，新角色引入"旁观者/受影响第三方"视角

**禁止**：新角色只是现有某个角色的"加强版"（如现有角色说"风险高"，新角色也说"风险高，千万别做"）——这是重复，不是增加。

### 3. 与现有角色的关系

- **不能重复**：新角色的立场+风格锁，必须跟现有所有角色有明显区分度（遮名测试必须能区分）
- **可以有矛盾**：新角色可以跟现有某个角色完全对立，这是好事
- **可以有联盟**：新角色可以跟现有某个角色立场接近，但必须从不同维度论证（如一个从"情感"角度，一个从"利益"角度）

### 4. 新角色生成流程

1. 石叔生成新角色卡（按角色卡标准格式）
2. 前置风控三问（详见`role-templates.md`）
3. 贴出新角色卡，让用户确认
4. 用户确认后，下一轮开始加入
5. **[强制] 石叔做对立验证**（遮名测试，见「量化判定标准·5. 角色对立性量化核验」），如果失败→重新生成该角色，直到通过

### 5. 讨论板更新

新角色加入后，讨论板标注：

```
**第N轮** （新增角色：[新角色名]）

[立] [新角色名]（[职位]）：[发言内容]
```

**【重要】每轮结束后，石叔必须自动执行以下两件事（不可跳过）**：

1. **更新【共识进度】**：用「全员共识判定」公式（见「量化判定标准·1. 全员共识判定」）评估当前共识进度，更新讨论板上的【共识进度】进度条（5个格子，0-5个▒）
2. **检查讨论饱和度**：用「讨论饱和度判定」公式（见「量化判定标准·6. 讨论饱和度判定」）评估是否饱和，若饱和→提前触发石叔总结

---

## 交互指令·语义兼容

以下指令支持语义匹配，不需要用户记住固定话术。

### 语义兼容表

| 用户可能说的 | 系统理解 | 触发动作 |
|---|---|---|
| "来一轮"/"再加一轮"/"继续" | 再加一轮讨论 | 继续执行下一轮 |
| "重新来"/"重置"/"换个思路" | 重置讨论，重新生成角色 | 清空讨论板，重新走流程 |
| "换角色"/"换个角色" | 替换某个角色 | 重新生成指定角色 |
| "总结一下"/"给个结论" | 提前总结 | 触发石叔总结（跳过剩余轮次） |
| "停"/"算了"/"不讨论了" | 终止讨论 | 触发石叔总结（基于已完成轮次） |

### 固定指令（原快捷指令）

| 用户说 | 我做 |
|---|---|
| "直接讨论" | 跳过诊断，直接生成角色开始讨论 |
| "再来一轮" | 当前阵容继续下一轮讨论 |
| "换一批角色" | 石叔重新生成4个角色 |
| "增加角色：[描述]" | 按「增加角色规则」执行（见上方） |
| "换讨论模式：[模式名]" | 切换到指定讨论模式（串行/结对/分组/圆桌） |
| "@某人" | 被点名角色必须正面回应（按点名机制详细规则执行） |
| "[新问题]" | 结束当前讨论，对新问题重新走流程 |

### 回退/撤回指令

| 用户可能说的 | 系统理解 | 触发动作 |
|---|---|---|
| "撤回"/"上一轮说错了" | 撤回上一轮发言 | 删除讨论板最后一轮，重新spawn该角色 |
| "这一轮重新来" | 重做当前轮 | 删除当前轮，重新spawn所有该轮角色 |

### 个性化配置指令

| 用户可能说的 | 系统理解 | 触发动作 |
|---|---|---|
| "我喜欢简短发言" | 全局偏好：发言≤30字 | 写入USER.md，后续所有角色遵守 |
| "我喜欢详细分析" | 全局偏好：发言≤100字 | 写入USER.md，后续所有角色遵守 |
| "记住这个角色" | 保存当前角色卡 | 写入role-templates.md，后续可复用 |

### 偏好记忆位置

用户偏好保存在：`{user_md}`（见 config.md，默认 `workspace/USER.md`）

格式：
```
## 讨论偏好

- 发言长度：≤30字（用户于2026-06-07设置）
- 默认讨论模式：结对辩论
- 记住的角色：王经理/张总/李阿姨/赵猎头
```

---


---

## 多人实时协作模式（v2.1 新增）

支持真人+AI混合讨论，真人可随时加入讨论。

### 真人角色标记

真人角色在讨论板中用 `[真]` 标记：
```
[立] [真] 张三（用户本人）：我觉得应该...
```

### 主持人模式

真人可接管石叔权限，成为主持人：

**触发**：用户说"我来主持"

**权限**：
- 决定讨论模式和轮次
- 决定角色发言顺序
- 决定何时终止讨论
- **不能**看到 AI 角色的"思考过程"（技术不可行）

**退出格式**：
```
🎤 **主持人：XXX** 已退出，石叔（AI）重新接管
```

### 真人插话处理（v2.2 更新 → v2.12 强化）

**回合制讨论**（串行/分组）：
- 真人插话 → 插入当前轮次末尾
- 不中断当前轮次，下一轮开始时石叔回应

**实时讨论**（圆桌/群聊）：
- 真人随时插话 → AI 实时响应
- 插话后，被插话的 AI 角色下一句必须正面回应真人

**v2.12 新增：插话引用深度分级**

所有角色必须引用真人插话，但引用深度分两级：
- **最低引用**（所有角色必须）：提及真人信息的至少1个具体点
- **核心引用**（每轮≥1角色必须）：把真人信息作为核心论据推进自己的论点，不是只提一下
- 如果spawn后所有角色都只是最低引用，该轮视作插话失效，需重跑

**v2.12 新增：石叔复述确认**

插话后增加确认步骤：
- 石叔用≤50字复述用户插话的核心信息
- 用户确认（"对"/"补充XXX"）后才spawn下一轮
- 如果用户纠正，石叔更新插话记录再spawn
- 简短插话（≤10字且无歧义）可跳过确认，石叔自行判断

**v2.12 新增：插话引导模板**

第1轮结束后，石叔在讨论板末尾追加插话引导：
- 列出石叔诊断中提到的关键变量（如收入、行业、老家城市、家庭因素）
- 提示用户"至少补1个变量"

**v2.12 新增：任意轮次间隙插话 + 衰减权重**

插话不限于第1轮后，可在任意轮次间隙插入：
- 第1轮后插话（权重1.0）：角色立场尚未固化，插话容易触发[让]
- 第2轮后插话（权重0.6）：角色已形成立场框架，核心引用率反而更高（插话变成弹药），但[让]触发率下降
- 第3轮后插话（权重0.3）：立场高度固化，插话主要被用来支撑已有立场而非修正

衰减不在引用率上，在**让步意愿**上。角色越晚听到真人信息，越倾向于用它强化自己已有立场而非改变立场。

⚠️ 石叔应根据插话时机调整spawn prompt：轮次越靠后，越需要在prompt中强调"根据真人信息重新审视你的立场，如果你的立场需要修正，用[让]标签"。
- 给出基本格式：`[真] 我在[城市]，做[行业]，月入[区间]，老家[城市]，想回因为[原因]`
- 用户可自由发挥，不强制套模板

**v2.12 新增：共识进度插话修正**

插话本身不直接计入共识进度：
- 插话轮次：共识不变，讨论板标注"🔄 真人信息待消化"
- 下一轮：只有被核心引用的角色立场移动才计入共识进度
- 避免用户产生"插话=自动加分"的误解

### 群聊场景适配

真人随时插话，AI 实时响应。适用于微信群、Discord 等场景。

### 线下会议场景适配

投影 AI 角色发言到屏幕上，现场讨论。适用于会议室投屏场景。

### 多人协作专用指令

| 用户说 | 触发动作 |
|---|---|
| "我来主持" | 进入主持人模式 |
| "我是XX，我想参与" | 以真人角色身份加入讨论 |
| "让我插一句" | 插入当前轮次（回合制）或实时响应（圆桌） |

### 讨论板格式升级（多人协作）

```
[立] [真] 张三（主持人）：[发言内容]
    🎤 **主持人模式**

[立] 赌徒（期货交易员）：[发言内容]
    ↳ [真] 张三：我不同意你的看法
```

### 归档格式升级（多人协作）

归档时新增字段：
- 真人参与者列表
- 主持人记录（谁主持了哪几轮）

---

---


















## 💬 反馈通道

用完了觉得哪里不好用？直接说 **"反馈：[你的意见]"** ，我会记下来下次改。

常见问题：
- 角色发言太模板化 → 反馈给我，我加软肋和触发后状态
- 讨论跑题了 → 反馈给我，我优化诊断环节
- 想要某个垂直行业的角色（如医疗/法律/教育）→ 反馈给我，我加进模板库

---

## L1/L2/L3 三级错误恢复机制

讨论过程中任何层级失败，都有对应恢复策略，不丢失已完成的工作。

### L1：单角色恢复（当前轮次内，个别角色 spawn 失败）

**触发条件**：某角色 spawn 后超时/报错/返回空内容。

**恢复步骤**：
1. 讨论板标注 `⚠️ [角色名] 本轮发言失败，正在重试...`
2. 立即重新 spawn 该角色（同一 prompt，不变）
3. 若重试仍失败（≤2 次），讨论板标注 `⚠️ [角色名] 连续失败，跳过本轮`
4. 后续轮次继续 spawn 该角色（不永久弃用）

**不丢失**：该角色之前轮次的发言保留在讨论板上。

---

### L2：整轮恢复（当前讨论内，整轮失败或严重漂移）

**触发条件**：
- 整轮 ≥3 个角色 spawn 失败
- 或连续 2 轮跑题（石叔判定"讨论跑题，未形成有效结论"）

**恢复步骤**：
1. 石叔在讨论板标注 `🔄 L2恢复：回退到第 N 轮状态`
2. 清除当前轮次所有发言（讨论板上保留记录，但标记为"已回退"）
3. 从上一轮结束后重新 spawn（角色阵容不变，prompt 微调：加入"⚠️ 上一轮跑题了，这次请紧扣原始问题"）
4. 若 L2 恢复后再次失败，升级到 L3

**不丢失**：讨论板历史记录全保留（含已回退轮次，划线标记）。

---

### L3：跨讨论恢复（整个讨论崩溃，或用户重新开始）

**触发条件**：
- 用户说"重置"/"重新来"/"换个思路"
- 或 L2 恢复失败（连续 2 次 L2 触发）

**恢复步骤**：
1. 石叔将当前讨论的核心结论（若有）写入归档（见下方「讨论归档机制」）
2. 清空讨论板（保留历史记录到 `memory/octopus-archive/YYYYMMDD-HHMM.md`）
3. 重新走完整流程：诊断 → 生成角色 → 讨论
4. 新讨论的第一轮，石叔 prompt 里加入"⚠️ 上一次讨论的结论是：[归档摘要]，这次请尝试不同角度"

**不丢失**：所有历史讨论归档到 `{archive_dir}`（见 config.md），可随时回溯。

---

### 恢复优先级

| 场景 | 触发 | 恢复级别 |
|---|---|---|
| 单个角色失败 | spawn 超时/报错 | L1（重试该角色） |
| 整轮失败 | ≥3 角色失败，或连续跑题 | L2（回退到上一轮） |
| 讨论崩溃 | 用户重置，或 L2 连续失败 | L3（归档 + 重新开始） |

---

## 讨论归档机制

每次讨论结束后（石叔总结完成），自动将核心结论写入知识库，供后续讨论引用。

### 归档内容

每次归档包含：
1. **问题**：原始用户问题
2. **结论**：石叔总结的六维框架结论（≤100 字）
3. **角色阵容**：本次讨论的 4 个角色名 + 核心立场
4. **关键转折点**：讨论中哪一轮发生了立场摇摆或共识浮现
5. **时间戳**：归档时间（ISO 8601）

### 归档格式

文件路径：`{archive_file}`（见 config.md，默认 `workspace/memory/octopus-archive/YYYYMMDD-HHMM.md`）

文件内容模板：
```markdown
# 讨论归档：[问题摘要]

**时间**：2026-06-07 13:21
**问题**：[原始用户问题]

## 结论

[石叔总结六维框架]

## 角色阵容

| 角色 | 立场 | 关键发言 |
|---|---|---|
| 王经理（大厂中层） | 支持提加薪 | "手里有offer再提" |
| 李阿姨（邻家长辈） | 反对冒险 | "现在工作不好找" |
| ... | ... | ... |

## 关键转折点

- 第 2 轮：王经理 @ 赵猎头，双方达成共识"有outside offer才提"
- 第 3 轮：李阿姨立场松动，从"反对"变为"有条件支持"

## 后续可引用

- 若再遇"加薪"问题，引用本次结论："需满足手上有offer或连续两季度KPI超30%"
```

### 归档触发时机

| 触发方式 | 说明 |
|---|---|
| 正常结束 | 石叔总结完成后，自动归档 |
| 提前终止 | 用户说"停"/"算了"，石叔总结基于已完成轮次，然后归档 |
| 用户重置 | 说"重新来"时，先归档当前讨论（哪怕未总结），再重置 |

### 归档后引用

后续讨论中，石叔诊断环节可以引用历史归档：
- 若新问题与某次归档高度相关（关键词匹配 ≥2 个），石叔在诊断里注明：
  `📂 引用归档 2026-06-07：上次讨论结论：[结论摘要]`
- 角色 spawn 时，prompt 里加入归档引用（若该角色与上次同名）：
  `⚠️ 上次讨论中你的立场是：[上次立场]，这次可以保持一致或刻意反转`

---



---


---

## 角色模板库管理规则（v2.4 新增）

### 模板格式定义

每个角色模板包含以下字段：

| 字段 | 必填 | 说明 |
|---|---|---|
| 模板ID | 是 | 唯一标识，格式：`组名-角色名`（如`商业组-赌徒`） |
| 名称 | 是 | 角色名（如`赌徒`） |
| 职位 | 是 | 角色职位（如`期货交易员`） |
| 立场 | 是 | 核心立场（1句话） |
| 性格 | 是 | 性格特征（影响发言风格） |
| 软肋 | 是 | 能让角色动摇的触发点 |
| 建议模型 | 是 | 该角色做建议时的思维模型 |
| 示例发言 | 是 | 1-2句典型发言（用于校验对立性） |
| 专业领域 | 否 | 该角色擅长的领域（v2.7 新增，v2.8 支持多选） |

### 模板操作流程

**新增模板**：
1. 石叔生成角色卡
2. 前置风控三问（见 `role-templates.md`）
3. 写入 `role-templates.md`
4. 更新 `CHANGELOG.md`

**删除模板**：
1. 确认该模板 ≥30 天未被使用
2. 从 `role-templates.md` 移除
3. 归档到 `role-templates-archive.md`（不删除，仅归档）

**修改模板**：
1. 修改 `role-templates.md` 中对应条目
2. 更新 `CHANGELOG.md`
3. 若修改了立场/性格/软肋，需重新跑遮名测试

**查看模板**：
1. 读取 `role-templates.md`
2. 或用标签筛选器查询（`scripts/tag_filter.py`）

### 从模板生成角色的流程

1. 从 `role-templates.md` 读取模板
2. 根据当前问题微调立场表述（保持核心立场不变）
3. 生成角色卡
4. 贴出角色卡让用户确认

### 模板库维护规则

- **定期审查**：每 30 天审查一次模板库，移除长期未使用的模板
- **去重**：相似度 >80% 的模板必须合并或删除其中一个
- **归档**：删除的模板不物理删除，归档到 `role-templates-archive.md`

---

---










## 角色标签筛选器

角色模板库中的每个角色都有 6 维标签，支持多条件组合查询。

### 标签体系

| 标签类别 | 可选值 |
|---|---|
| 立场标签 | 激进 / 保守 / 中立 / 折中派 |
| 视角标签 | 当事人 / 管理者 / 执行层 / 第三方 / 专业顾问 |
| 风格标签 | 数据型 / 经验型 / 情绪型 / 务实型 |
| 适用场景 | 决策 / 评估 / 规划 / 纠纷 |
| 冲突强度 | 高对立 / 中度博弈 / 温和探讨 |
| 状态标签 | 常用 / 低频 / 待优化 |

### 使用方法

在命令行运行 scripts/tag_filter.py，支持任意数量的条件（AND 关系，模糊匹配）：

`ash
# 查所有角色
python scripts/tag_filter.py

# 单条件：查「数据型」风格的角色
python scripts/tag_filter.py 数据型

# 多条件：商业组 + 数据型 + 高对立
python scripts/tag_filter.py 商业组 数据型 高对立

# 其他示例
python scripts/tag_filter.py 激进
python scripts/tag_filter.py 常用
python scripts/tag_filter.py 高对立
`

### 石叔调用时机

石叔在「诊断阶段」可以调用标签筛选器辅助选择角色：
1. 用户问题明确 → 直接选模板库角色
2. 用户问题模糊 / 需要特定对立组合 → 用标签筛选器查询
3. 查询结果 0 个 → 走动态生成流程

> 📌 标签筛选器是可选工具，不是必须步骤。石叔凭经验直接选角色也是允许的。

