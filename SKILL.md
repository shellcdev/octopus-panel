> 📌 版本：v2.13 | 更新：2026-06-08 | 维护者：石叔
> 🔧 运行时路径配置：见 `config.md`，修改该文件即可自定义路径，无需改动 SKILL.md

name: octopus-panel
description: 多角色圆桌讨论系统（八爪议事厅）。当用户提出任何需要多方视角碰撞的问题时触发：决策问题、方案评估、诊断类问题、人生规划、职业选择、商业判断、技术选型、产品策略等。支持多种讨论形式（串行/结对/分组/圆桌）。讨论结束后自动生成结构化总结（石叔总结）。用户可通过"再来一轮"继续，或"换一批角色"重新生成。

> 🐙 **第一次用？** 直接把你的困惑/决策/两难问题发给我就行，剩下的交给我。
>
> 聊的过程中你可以随时：
> - `@某个角色` 让他必须回答你
> - 说"再来一轮"让讨论继续
> - 说"总结一下"提前要结论
> - 说"换角色"换一批人上来吵
>
> 不用记任何指令，说人话就行。

# 八爪议事厅 · 总览

## 两种使用方式

**直接提问**（推荐）：用户直接说问题，我立刻进入流程，不需要额外指令。

**带指令提问**：可以说"直接讨论"、"再来一轮"、"换一批角色"、"增加角色"、"换讨论模式：[模式名]"。

## 四种讨论模式

石叔根据问题复杂度选择合适的讨论模式。**默认使用串行模式**，其他模式在问题适合时由石叔主动建议，或用户明确要求时切换。

### 模式1：串行发言（默认）

**适用**：大多数问题，特别是需要每个角色都表态的情况。

**流程**：石叔编排顺序 → 逐个spawn → 每个角色能看到前面所有人的发言 → 一轮结束贴讨论板。

**特点**：稳，每个角色都有发言机会，但交锋感弱。

### 模式2：结对辩论

**适用**：两方对立明显的问题（如"自研vs买API"、"回老家vs留大城市"）。

**流程**：
1. 石叔把4个角色分成2对（A↔B，C↔D）
2. 每对内部先互怼1轮（A说→B怼→A再回1句）
3. 两对交叉（A↔C，B↔D）再1轮
4. 石叔总结

**特点**：冲突密度高，适合立场鲜明的二选一问题。

**触发条件**：石叔诊断时发现"核心矛盾可以归为两派对立"。

**并行spawn规则（v2.12 新增）**：第1轮[立]时，同对角色可并行spawn（A+B同时出，C+D同时出），因为第1轮还未看到对方发言。第2轮及以后必须串行，因为需要引用对方原话。

### 模式3：分组讨论

**适用**：复杂问题，需要"先分组酝酿立场，再全体碰撞"的情况。

**流程**：
1. 石叔把4个角色分成2组（组A：角色1+2，组B：角色3+4）
2. 每组内部讨论1轮（组内两个角色先交换意见，形成组内共识）
3. 两组对抗1轮（组A代表发言 → 组B代表发言 → 交叉质疑）
4. 全员开放讨论1轮
5. 石叔总结

**特点**：先形成阵营，再对抗，适合多维复杂问题。

### 模式4：圆桌模式（全员可见）

**适用**：需要高度协作、信息对称的问题（如"怎么一起解决这个问题"）。

**流程**：
1. 石叔先发一个"开场白"，把所有角色的背景和立场同时贴出来
2. 所有角色同时看到彼此的立场
3. 自由发言2轮（不按固定顺序，角色可以选择回应谁）
4. 石叔总结

**注意**：圆桌模式目前受技术限制——无法实现真正的"同时看到"，只能用"把所有前面发言一次性贴给所有人"来模拟。效果略打折扣，但比串行更贴近"圆桌"感。

### 模式5：串行+结对混合（v2.9 新增）

**适用**：需要先让每个角色表态，再让对立角色深度交锋的问题。

**流程**：
1. 前2轮串行表态（每个角色说立场）
2. 后2轮结对互怼（立场对立的角色两两互怼）
3. 石叔总结

### 模式6：分组+圆桌混合（v2.9 新增）

**适用**：复杂多维度问题，需要"组内酝酿 → 组间对抗 → 全员自由交流"。

**流程**：
1. 石叔把角色分成2组（组A/组B），组内讨论1轮（形成组内共识）
2. 两组对抗1轮（组A代表 vs 组B代表）
3. 全员圆桌自由交流1轮（不按固定顺序，角色可以选择回应谁）
4. 石叔总结

### 模式7：结对+圆桌混合（v2.9 新增）

**适用**：需要先两两辩论，再开放全员补充的问题。

**流程**：
1. 石叔把4个角色分成2对（A↔B，C↔D），每对辩论1轮
2. 全员圆桌自由交流1轮（不按固定顺序）
3. 石叔总结

> 📌 混合模式自动沿用原有格式、标签、标注，无需重新适配。

### 模式8：开杠极速模式（v2.14 新增）

**适用**：需要快速得到两个对立观点碰撞的场景（如"我该不该租房，快给我两个极端意见"）。

**流程**：
1. 石叔诊断（≤100字）
2. spawn 2个角色（对立立场各1）
3. 每个角色发言1轮（≤50字）
4. 石叔立刻给判断（≤100字）

**特点**：快，总耗时约30秒，适合快速决策场景。

**限制**：
- 不适用六帽滤镜（1轮太短）
- 不生成观点迁移图（仅1轮）
- 石叔总结用简化版（只给结论+核心理由）


## 讨论模式·智能触发逻辑

石叔根据问题诊断结果，自动判断是否需要切换模式。以下是触发条件（量化）。

### 默认：串行发言

所有问题默认走串行模式，除非命中以下触发条件。

### 触发条件（满足即建议切换）

| 目标模式 | 触发条件（量化） | 示例 |
|---|---|---|
| 结对辩论 | 石叔诊断：核心矛盾可归为两派对立 AND 问题为二选一 | "自研vs买API" "回老家vs留大城市" |
| 分组讨论 | 问题决策维度≥4个 AND 角色数≥5个 | "是否接受海外offer（维度：薪资/签证/家庭/职业）" |
| 圆桌模式 | 问题为协作型（"怎么一起解决XX"）AND 需要信息对称 | "怎么一起提升团队效率" |

### 切换规则（用户要求切换时）

用户说"换讨论模式：[模式名]"，石叔直接切换，不需要重新诊断。

切换时：
1. 已生成的角色保留
2. 已完成的轮次保留（讨论板保留）
3. 按新模式重新编排剩余轮次

### 模式不兼容提示

以下情况石叔主动提示用户"当前模式可能不合适"：
- 结对辩论 + 角色数≠4（无法平分两对）→ 建议改为串行或分组
- 分组讨论 + 角色数<4（无法分两组）→ 建议改为串行
- 圆桌模式 + 问题为对立型（"该不该XX"）→ 建议改为结对或串行

## 核心流程（5步）

```
用户提问
    ↓
① 石叔诊断（≤200字）+ 选择讨论模式
    ↓
② 石叔生成角色（4张卡，≤400字）+ 编排规则
    ↓
③ 按所选模式讨论（串行/结对/分组/圆桌）
    ↓
③.5 第1轮结束后 → 石叔出插话引导（列出缺失变量+提示补1个）→ 用户插话或跳过
    ↓
④ 每2轮石叔做一次阶段性小结（仅长轮次≥3轮时触发）
    ↓
⑤ 石叔总结（六维框架 + ≤100字最终判断）
```

## 文件索引

| 优先级 | 文件 | 内容 | 何时读 |
|---|---|---|---|
| 🔴 必读 | `SKILL.md` | 总览 + 硬性/柔性规则 + 讨论模式 + 交互指令 | 第一次使用前 |
| 🔴 必读 | `references/jargon.md` | 术语通俗释义 | 第一次使用前 |
| 🟡 建议 | `references/rules-discussion.md` | 点名机制、讨论板格式、阶段性小结、收敛机制、异常场景 | 讨论进行中 |
| 🟡 建议 | `references/summary-format.md` | 石叔总结格式、动态终止、量化判定标准 | 总结阶段 |
| 🟡 建议 | `references/roles-rules.md` | 角色选择、增加角色、模板库管理、标签筛选器 | 生成/替换角色时 |
| 🟡 建议 | `references/rules-collab.md` | 多人协作、真人插话、L1/L2/L3恢复、归档机制 | 协作场景 |
| 🟡 建议 | `references/templates.md` | 角色卡/Prompt/讨论板/小结标准格式 | 生成角色前 |
| 🟡 建议 | `references/role-templates.md` | 已验证的角色模板库 + 前置风控三问 | 生成角色时 |
| 🟢 可选 | `references/discussion-examples.md` | 7个完整讨论案例（含边界场景） | 质量核对或格式参照 |

## 石叔总结格式分档

总结格式根据讨论类型自动选择，详见 `references/summary-format.md`：

| 讨论类型 | 格式 | 触发条件 |
|---|---|---|
| 对立型（该不该XX） | 正反方力量对比 | 默认 |
| 协作型（怎么XX） | 可行方案排序 | 默认 |
| 对峙型（框架不兼容） | 变量检查单 | 共识连续2轮≤30%且零[让] |

对峙型格式输出"变量检查单"而非最终判断，与"讨论饱和无法收敛"出口联动。

> 所有总结格式末尾均附【⑦ 你的选项】（A/B/C 三选一），详见 `references/summary-format.md` ⑦节。



## 术语速查

- **风格约束锁**：角色说话的禁忌，违反即角色漂移
- **软肋**：能让角色动摇或失控的触发点
- **触发后状态**：软肋被戳中后，角色的行为变化梯度（沉默/摇摆/激动）
- **讨论板**：实时更新的发言记录，贴在回复里。升级版含观点标签、冲突高亮、违规标注
- **石叔**：总控调度器，只拆结构不站队
- **阶段性小结**：每2轮石叔做一次梳理（仅≥3轮时触发），不是完整总结，只是"现在话题演化到哪了"

详细释义见 `references/jargon.md`。

## ⚠️ 硬性规则（不可违反）

1. **必须按模式规则spawn**：串行模式逐个spawn，结对模式第1轮同对角色可并行spawn（v2.12）、后续轮次串行
2. **石叔只拆结构**：不发表自己的观点，不站队，不下场辩论
3. **风格约束锁必须执行**：角色发言违反约束锁 → 在下一轮prompt里加入⚠️提醒，讨论板对应发言后加⚠️标注
4. **讨论板实时更新**：每个子Agent完成后立即贴出，不等全部说完。升级版讨论板必须包含：观点标签+冲突高亮+违规标注
5. **NO_REPLY规则**：子Agent完成之间，用NO_REPLY等待
6. **@点名机制必须正面回应**：被点名的角色下一轮必须直接引用对方原话再反驳，禁止答非所问
7. **阶段性小结**（≥3轮时强制）：每2轮石叔必须做一次阶段性小结，梳理当前话题演化路径
8. **spawn prompt 必须注入💡观点摘要**（第N≥2轮）：spawn每个角色前，必须填充 `templates.md` Prompt模板中的「💡 前一轮各角色核心观点」节——用1句话摘要每个角色上一轮的核心观点，禁止只写"前几轮回顾"一句话

## 🔧 柔性规则（可调整）

以下规则石叔可根据问题类型灵活调整，不需要每次走标准流程：

| 规则 | 默认 | 可调整为 |
|---|---|---|
| 角色数量 | 4个 | 3-6个（问题越复杂可越多） |
| 讨论轮次 | 2轮 | 1-4轮（简单问题1轮，复杂问题3-4轮） |
| 发言字数（按类型） | 直觉型≤60 / 平衡型≤80 / 分析型≤100 | 快速模式统一≤50；复杂议题分析型可临时放宽至120（需注明理由） |
| 发言字数（全局下限） | ≥20字 | 禁止1-2个词敷衍，不足20字必须展开一句话 |
| 诊断字数 | 200字以内 | 复杂问题可放宽至300字 |
| 讨论模式 | 串行 | 结对/分组/圆桌（根据问题类型） |
| 点名机制 | 无 | 可开启（用户@某人时触发，或角色互相@） |
| 终止机制 | 标准2轮 | 石叔可根据共识/饱和提前终止 |
| 阶段性小结 | ≥3轮时触发 | 可改为每N轮一次（N=1~3） |
| 快速模式 | 关闭 | 开启：跳过观点萃取+共识进度条+彩色标识，只保留发言板+石叔总结 |

> 📝 以上均为建议值，不是硬限制，但石叔应尽量引导角色精简发言。

### 快速模式（v2.12 新增）

当用户说"快速过一轮"或石叔判断问题简单度≥7/10时，可开启快速模式：
- **跳过**：观点萃取、共识进度条、彩色标识、插话引导
- **保留**：发言板 + 石叔总结（六维框架）
- **默认**：关闭（走完整流程）
- 快速模式下的讨论板只用简单分隔线区分轮次，不做结构化排版

## 详细规则索引

以下规则已拆分到独立文件，石叔在讨论过程中按需引用：

| 文件 | 内容 | 触发时机 |
|---|---|---|
| `references/rules-discussion.md` | 点名机制、角色插入、讨论板格式、阶段性小结、收敛机制、异常场景 | 讨论进行中 |
| `references/summary-format.md` | 石叔总结格式、动态终止、量化判定标准 | 总结阶段 / 判定违规时 |
| `references/roles-rules.md` | 角色选择、增加角色、模板库管理、标签筛选器 | 生成/替换角色时 |
| `references/rules-collab.md` | 多人协作、真人插话、L1/L2/L3恢复、归档机制 | 协作场景 / 错误恢复时 |

## 交互指令·语义兼容

以下指令支持语义匹配，不需要用户记住固定话术。

### 语义兼容表

| 用户可能说的 | 系统理解 | 触发动作 |
|---|---|---|
| "来一轮"/"再加一轮"/"继续" | 再加一轮讨论 | 继续执行下一轮 |
| "重新来"/"重置"/"换个思路" | 重置讨论，重新生成角色 | 清空讨论板，重新走流程 |
| "换角色"/"换个角色" | 替换某个角色 | 重新生成指定角色 |
| "总结一下"/"给个结论" | 提前总结 | 触发石叔总结（跳过剩余轮次） |
| "停"/"算了"/"不讨论了" | 终止讨论 | 触发石叔总结（基于已完成轮次） |

### 固定指令（原快捷指令）

| 用户说 | 我做 |
|---|---|
| "直接讨论" | 跳过诊断，直接生成角色开始讨论 |
| "再来一轮" | 当前阵容继续下一轮讨论 |
| "换一批角色" | 石叔重新生成4个角色 |
| "增加角色：[描述]" | 按「增加角色规则」执行（见上方） |
| "换讨论模式：[模式名]" | 切换到指定讨论模式（串行/结对/分组/圆桌） |
| "@某人" | 被点名角色必须正面回应（按点名机制详细规则执行） |
| "[新问题]" | 结束当前讨论，对新问题重新走流程 |

### 回退/撤回指令

| 用户可能说的 | 系统理解 | 触发动作 |
|---|---|---|
| "撤回"/"上一轮说错了" | 撤回上一轮发言 | 删除讨论板最后一轮，重新spawn该角色 |
| "这一轮重新来" | 重做当前轮 | 删除当前轮，重新spawn所有该轮角色 |

### 个性化配置指令

| 用户可能说的 | 系统理解 | 触发动作 |
|---|---|---|
| "我喜欢简短发言" | 全局偏好：发言≤30字 | 写入USER.md，后续所有角色遵守 |
| "我喜欢详细分析" | 全局偏好：发言≤100字 | 写入USER.md，后续所有角色遵守 |
| "记住这个角色" | 保存当前角色卡 | 写入role-templates.md，后续可复用 |

### 偏好记忆位置

用户偏好保存在：`{user_md}`（见 config.md，默认 `workspace/USER.md`）

格式：
```
## 讨论偏好

- 发言长度：≤30字（用户于2026-06-07设置）
- 默认讨论模式：结对辩论
- 记住的角色：王经理/张总/李阿姨/赵猎头
```

## 💬 反馈通道

用完了觉得哪里不好用？直接说 **"反馈：[你的意见]"** ，我会记下来下次改。

常见问题：
- 角色发言太模板化 → 反馈给我，我加软肋和触发后状态
- 讨论跑题了 → 反馈给我，我优化诊断环节
- 想要某个垂直行业的角色（如医疗/法律/教育）→ 反馈给我，我加进模板库
